<%= form_for(bulletin_boards1) do |f| %>
  <% if bulletin_boards1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bulletin_boards1.errors.count, "error") %> prohibited this bulletin_boards1 from being saved:</h2>

      <ul>
      <% bulletin_boards1.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.file_field :picture %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
